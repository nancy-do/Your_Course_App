<div class= "container">
    <h1>Courses in category <%=@category.name%></h1>
    <div class="list-group">
        <% @courses.each do |course| %>
            <%= link_to course.name, "#", class:"list-group-item" %>
        <% end %>
    </div>
</div>
